# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Web app deploy (Push->Master)
'on':
  push:
    paths:
      - 'hera_app/**'
    branches:
      - master

env:
  flutter_channel: 'beta'
jobs:
  
  #! Build and deploy Civilio web app
  app_build_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Cache Flutter dependencies
        uses: actions/cache@v1
        with:
          path: /opt/hostedtoolcache/flutter
          key: ${{ runner.OS }}-flutter-install-cache-${{ env.flutter_channel }}    
      - uses: subosito/flutter-action@v1
        with:
          channel: '${{ env.flutter_channel }}'
      - name: Build Civilio web app
        run: |
          flutter config --enable-web
          cd hera_app
          flutter pub get
          flutter build web --release
          cp -R build/web web/public
      - name: Deploy Civilio web app
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_CIVILIO_CI }}'
          projectId: civilio-ci
          entryPoint: hera_app/web/
          channelId: live
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
